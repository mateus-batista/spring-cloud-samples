spring:
  application:
    name: admin-server
  profiles:
    active:
      - secure
  cloud:
    config:
      uri: http://localhost:8888
      username: config_client
      password: secret

server:
  port: 8500