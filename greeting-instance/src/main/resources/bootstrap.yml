spring:
  application:
    name: greeting-instance
  cloud:
    config:
      uri: http://localhost:8888
      username: config_client
      password: secret
  security:
    user:
      name: admin
      password: secret

server:
  port: 8081