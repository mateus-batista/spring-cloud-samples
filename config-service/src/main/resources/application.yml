spring:
  application:
    name: config-service
    
server:
  port: 8888

---

spring:
  profiles: default
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mateus-batista/config-repo.git
 
---
  
spring:
  profiles: docker
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mateus-batista/config-repo.git
      